---
kind: Service
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: radarr
spec:
  ports:
  - port: 7878
    name: webui
    protocol: TCP
  selector:
    app.kubernetes.io/component: radarr
